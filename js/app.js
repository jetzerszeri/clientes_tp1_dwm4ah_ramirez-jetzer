import { findByCategory, renderServicesBtns, updateServicesList } from './services.js';

const app = {
    services : {
        findByCategory,
        updateServicesList,
        renderServicesBtns,
    }
}

export default app;